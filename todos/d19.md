# Day 19 — Admin UX & localization (from request04)

> Six admin-side polish items raised after using the admin console
> (`todos/request04.md`). All additive UI/query refinements — no business-rule
> changes.

**Decisions (confirmed):** calendar shows only `status='active'` vehicles with an
assigned, active driver · bookings get quick tabs `Needs action | Next 2 days |
All` + smart sort (full-day → pending → soonest) · one new export: **Pending
approvals queue** · reports become preview-then-export.

---

## 🎯 Goal

A calmer, more scannable admin console fully translated in EN / 中文 / Tiếng Việt.

---

## ✅ Task status

- [x] T1 — Calendar shows active-driver vehicles only
- [x] T2 — Slot control lives in Vehicles, not Users (extract `SlotsModal`)
- [x] T3 — Bookings quick tabs + smart sort
- [x] T4 — Booking-trend x-axis decluttered (30-day view)
- [x] T5 — Reports preview-then-export + Pending approvals export
- [x] T6 — Localize all admin text (dashboard first)

---

## 🔧 Implementation details

- **T1** `server/controllers/calendarController.js`: join `users`, filter `v.status='active' AND u.is_active=1`. `AdminCalendar.jsx`: drop maintenance legend/branch.
- **T2** New `client/src/components/SlotsModal.jsx` (`{driver,onClose}`); reuse `/admin/users/:id/slots`. `AdminVehicles.jsx` opens it with `{user_id: v.driver_user_id, name: v.driver_account}`. Strip inline modal + Slots button from `AdminUsers.jsx`.
- **T3** `bookingController.list`: optional `within` (days) + `sort=priority` → `ORDER BY (booking_type='full_day') DESC, (status='pending') DESC, booking_date ASC, slot_start ASC`. `AdminBookings.jsx`: `Segmented` tabs drive params; status `Select` only on All tab.
- **T4** `AdminDashboard.jsx` trend `<XAxis>`: `interval="preserveStartEnd"`, `minTickGap={24}`, `tickFormatter` → `MM-DD`.
- **T5** Refactor `exportController.js` into per-report builders used by both `download` (xlsx) and `preview` (JSON); add `pending` builder. Routes: `GET /export/:report` + `GET /export/:report/preview`. `AdminReports.jsx`: selector + range → preview Table → Export button.
- **T6** `client/src/i18n.jsx`: add `admin.*` to en/zh/vi (reuse `nav.*/status.*/f.*/common.*`). Convert AdminDashboard, AdminBookings, AdminReports, AdminCalendar, AdminVehicles, AdminUsers, SlotsModal, AdminFeedback, AdminSettings.

---

## Verify before closing

- Calendar: only active vehicles w/ active driver; no maintenance/unassigned rows.
- Slots: button gone from Users, present on Vehicles rows w/ a driver; add/edit/remove round-trips.
- Bookings: `Needs action` shows pending w/ full-day on top; `Next 2 days` scoped; `All` = old behavior + status filter.
- Dashboard 30-day trend: x-axis labels readable.
- Reports: pick report → table preview → Export to Excel matches; Pending report lists pending w/ phone.
- i18n: toggle all three langs — no hardcoded English on admin screens. Client builds.
