I want admin first come to page in Dashboard tab, should have a popup tell you have how many full day bookings, slot bookings. Press ok go to booking tab with filter of new booking.
Now slot bookings no need admin to approve but still can reject if there is some need for full day booking

Now full day booking can choose more than 1 day. 

in Admin/booking tab, now allow admin to show any important information and hide some colunns, re-order columns

allow bookings tab to group by day, allow to hide completed/old booking (pass today)

in admin notification, if i click on some booking notification, there should be a popup to show, and button for admin to action (if any)

in admin calendar, there should be a grid view, click on each day willl show list of booking, click on each booking should open popup/side view

Now allow admin to create account, and write password for that user
